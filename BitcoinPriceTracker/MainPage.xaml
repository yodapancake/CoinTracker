<Page
    x:Class="BitcoinPriceTracker.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BitcoinPriceTracker"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <StackPanel Background="LightBlue">
        <StackPanel>
            <Image x:Name="ResultImage" Width="200" Height="200" Source="{x:Bind v.Image}" HorizontalAlignment="Center"/>
            <TextBlock x:Name="coin" Text="{x:Bind v.Coin, Mode=OneWay}" 
                       FontSize="52" Foreground="White" HorizontalAlignment="Center"/>
            <TextBlock x:Name="currency" Text="{x:Bind v.Currency, Mode=OneWay}" 
                       FontSize="36" Foreground="White" HorizontalAlignment="Center"/>
            <TextBlock x:Name="price" Text="{x:Bind v.Price, Mode=OneWay}" 
                       FontSize="24" Foreground="White" HorizontalAlignment="Center"/>
        </StackPanel>

    </StackPanel>
</Page>
