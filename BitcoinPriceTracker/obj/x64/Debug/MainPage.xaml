<Page x:ConnectionId='1'
    x:Class="BitcoinPriceTracker.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BitcoinPriceTracker"
    xmlns:data="using:BitcoinPriceTracker.ViewModels"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Page.Resources>
        <Style TargetType="TextBlock" x:Key="White_textblock">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="40"/>
            <Setter Property="FontFamily" Value="Helvetica"/>
        </Style>
        <Style TargetType="TextBlock" x:Key="Black_textblock">
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="FontSize" Value="40"/>
            <Setter Property="FontFamily" Value="Helvetica"/>
        </Style>
    </Page.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>


        <Image x:ConnectionId='2' x:Name="BackgroundImage" Grid.RowSpan="2" Source="Assets/polygon-1920x1080-4k-hd-wallpaper-orange-red-blue-background-pattern-225 (1).jpg" Stretch="UniformToFill"/>

        <ScrollViewer x:ConnectionId='3' IsEnabled="True"                                        />
        <ListView x:ConnectionId='4' Name="MyListview" Grid.Row="1" Grid.Column="1"                                           HorizontalAlignment="Center"                                ScrollViewer.HorizontalScrollBarVisibility="Visible" ScrollViewer.HorizontalScrollMode="Enabled">
            <ListView.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Horizontal"/>
                </ItemsPanelTemplate>
            </ListView.ItemsPanel>
            <ListView.ItemTemplate>
                <DataTemplate                                  >
                    <StackPanel x:ConnectionId='9' Margin="20,20,20,20" HorizontalAlignment="Stretch" Orientation="Vertical">
                        <Image x:ConnectionId='10' x:Name="ResultImage"                                                                                                                      HorizontalAlignment="Center"/>
                        <RelativePanel HorizontalAlignment="Center">
                            <Button x:ConnectionId='13' Name="Flyout_menu_button"                                  Width="50" Height="50" FontFamily="Segoe MDL2 Assets" Content="&#xE70D;" Background="White">
                                <Button.Flyout>
                                    <Flyout Placement="Bottom">
                                        <StackPanel Orientation="Vertical">
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="24 Hour Change: %" Style="{StaticResource Black_textblock}"/>
                                                <TextBlock x:ConnectionId='18' Name="24hourChangeTextblock"                                      Style="{StaticResource Black_textblock}"                                />
                                            </StackPanel>
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="24 Hour Volume: " Style="{StaticResource Black_textblock}"/>
                                                <TextBlock x:ConnectionId='17' Name="24hourVolumeTextblock"                                    Style="{StaticResource Black_textblock}"                                />
                                            </StackPanel>
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="Market Cap: " Style="{StaticResource Black_textblock}"/>
                                                <TextBlock x:ConnectionId='16' Name="CoinSupplyTextblock"                                 Style="{StaticResource Black_textblock}"                                />
                                            </StackPanel>
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="Coin Supply:" Style="{StaticResource Black_textblock}"/>
                                                <TextBlock x:ConnectionId='15'                                  Style="{StaticResource Black_textblock}"/>
                                            </StackPanel>
                                        </StackPanel>
                                    </Flyout>
                                </Button.Flyout>
                            </Button>
                            <TextBlock x:ConnectionId='14' x:Name="currency" Margin="20,0,0,0" 
                                                                              
                                       Style="{StaticResource White_textblock}" 
                                       HorizontalAlignment="Center"
                                       RelativePanel.AlignVerticalCenterWith="Flyout_menu_button"
                                       RelativePanel.RightOf="Flyout_menu_button"/>
                        </RelativePanel>
                        <TextBlock x:ConnectionId='11' x:Name="coin"                                               Style="{StaticResource White_textblock}" HorizontalAlignment="Center"/>
                        <TextBlock x:ConnectionId='12' x:Name="price"                                             Style="{StaticResource White_textblock}" HorizontalAlignment="Center"/>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <Button x:ConnectionId='5' Name="Hamburger_button"                                Width="100" Height="60" FontSize="40" FontFamily="Segoe MDL2 Assets" Content="&#xE700;" Background="White"/>
        <SplitView x:ConnectionId='6' Name="HambugerSplit" Grid.Row="1" Grid.Column="0" DisplayMode="CompactOverlay" OpenPaneLength="200" CompactPaneLength="0" HorizontalAlignment="Left">
            <SplitView.Pane>
                <StackPanel>
                    <ScrollViewer/>
                    <ListView x:ConnectionId='7' SelectionMode="Single"                                           ScrollViewer.VerticalScrollMode="Enabled">
                        <ListView.ItemTemplate>
                            <DataTemplate                                  >
                                <TextBlock x:ConnectionId='8'                           Style="{StaticResource Black_textblock}"/>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </StackPanel>
            </SplitView.Pane>
        </SplitView>
    </Grid>
</Page>

